schema_version: 1
module:
  id: interactive-brokers
  name: Interactive Brokers
  category: brokerage
  policy_class: sensitive_account
  status: research
  owners: [HCloudLab]
  description: Proof fixture for brokerage/account traffic; not a complete IBKR ruleset.
release_policy:
  minimum_evidence: verified
  allowed_ownership: [first_party, dedicated_third_party]
  required_tests: [web_access, auth_flow]
  forbid_unresolved_policy_conflicts: true
rules:
  - id: interactive-brokers.domain-suffix.interactivebrokers-com
    match: {type: domain_suffix, value: interactivebrokers.com}
    ownership: first_party
    purpose: service_core
    provenance: {kind: first_party, source: official_service, reference: null, observed_at: null}
    evidence: {level: candidate, methods: [official_domain]}
    validation: {state: untested, last_tested: null, tests: []}
    conflicts: []
    notes: Seed fixture; account/auth/API/trading dependencies require HCloudLab validation.
