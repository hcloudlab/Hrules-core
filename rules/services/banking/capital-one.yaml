schema_version: 1
module:
  id: capital-one
  name: Capital One
  category: banking
  policy_class: sensitive_account
  status: validating
  owners: [HCloudLab]
  description: Capital One first-party routing module with recorded Clash Verge runtime routing evidence. Authentication-flow validation remains outside the RC gate.
release_policy:
  minimum_evidence: verified
  allowed_ownership: [first_party, dedicated_third_party]
  required_tests: [web_access, auth_flow]
  forbid_unresolved_policy_conflicts: true
rules:
  - id: capital-one.domain-suffix.capitalone-com
    match: {type: domain_suffix, value: capitalone.com}
    ownership: first_party
    purpose: service_core
    provenance: {kind: hcloudlab_observation, source: clash_verge_final_acceptance, reference: "capitalone.com routed through the dedicated banking policy", observed_at: "2026-09-16"}
    evidence: {level: corroborated, methods: [official_domain, connection_log, session_observation]}
    validation: {state: partial, last_tested: "2026-09-16", tests: [web_access]}
    conflicts: []
    notes: Runtime routing identification is accepted for RC publication. This does not claim authentication or account-risk behavior is validated.
