schema_version: 1
module:
  id: gemini
  name: Gemini
  category: ai
  policy_class: region_preferred
  status: research
  owners: [HCloudLab]
  description: Proof fixture for an AI service whose Google dependencies require overlap/dependency analysis.
release_policy:
  minimum_evidence: verified
  allowed_ownership: [first_party, dedicated_third_party]
  required_tests: [web_access, auth_flow]
  forbid_unresolved_policy_conflicts: true
rules:
  - id: gemini.domain-suffix.gemini-google-com
    match: {type: domain_suffix, value: gemini.google.com}
    ownership: first_party
    purpose: service_core
    provenance: {kind: first_party, source: official_service, reference: null, observed_at: null}
    evidence: {level: candidate, methods: [official_domain]}
    validation: {state: untested, last_tested: null, tests: []}
    conflicts: []
    notes: Seed fixture; broader Google authentication/API dependencies are intentionally not claimed here yet.
