schema_version: 1
module:
  id: coinbase
  name: Coinbase
  category: crypto
  policy_class: sensitive_account
  status: validating
  owners: [HCloudLab]
  description: Coinbase first-party routing module with recorded Clash Verge runtime routing evidence. Authentication-flow validation remains outside the RC gate.
release_policy:
  minimum_evidence: verified
  allowed_ownership: [first_party, dedicated_third_party]
  required_tests: [web_access, auth_flow]
  forbid_unresolved_policy_conflicts: true
rules:
  - id: coinbase.domain-suffix.coinbase-com
    match: {type: domain_suffix, value: coinbase.com}
    ownership: first_party
    purpose: service_core
    provenance: {kind: hcloudlab_observation, source: clash_verge_final_acceptance, reference: "login.coinbase.com routed through the crypto policy", observed_at: "2026-09-16"}
    evidence: {level: corroborated, methods: [official_domain, connection_log, session_observation]}
    validation: {state: partial, last_tested: "2026-09-16", tests: [web_access]}
    conflicts: []
    notes: Runtime routing identification is accepted for RC publication. This does not claim authentication or account-risk behavior is validated.
